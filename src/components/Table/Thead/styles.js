import styled from 'styled-components';

import colors from '../../../styles/colors';

export const StyledThead = styled.tbody`
  color: ${colors.darkSecond};
  font-weight: bold;
  font-size: 14px;
`;
